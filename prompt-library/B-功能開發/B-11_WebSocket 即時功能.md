# B-11　WebSocket 即時功能

> **類別**：B. 功能開發

---

```
實作 WebSocket 即時功能：
功能：[聊天室 / 即時通知 / 協作編輯 等]
框架：[Socket.io / 原生 WebSocket / Server-Sent Events]
要包含：
- 連線/斷線處理
- 自動重連（指數退避）
- 房間管理（如適用）
- 事件型別的 TypeScript 定義
```
