# B-09　角色與權限系統

> **類別**：B. 功能開發

---

```
實作 RBAC（角色為基礎的存取控制）：
角色清單：[admin / editor / viewer 等]
資源清單：[各角色能操作的功能]
實作方式：
- 資料庫：roles, permissions, user_roles 資料表
- 中介層：checkPermission(resource, action)
- 前端：根據權限隱藏/停用 UI 元素
```
