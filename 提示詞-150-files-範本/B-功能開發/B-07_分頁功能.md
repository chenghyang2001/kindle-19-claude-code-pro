# B-07　分頁功能

> **類別**：B. 功能開發

---

```
為 [端點或列表] 實作分頁：
方式：[Cursor-based（推薦大資料集）/ Offset-based]
預設每頁：[數量] 筆
API 回傳格式：
{
  data: [...],
  pagination: { total, page, limit, hasNext, nextCursor }
}
前端：無限捲動 或 頁碼導航（擇一）。
```
