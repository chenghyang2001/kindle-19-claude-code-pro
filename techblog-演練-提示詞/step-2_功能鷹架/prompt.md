# Step 2 使用的提示詞
> 來源：`B-功能開發/B-01_新功能鷹架.md`

---

```
建立「文章收藏（Bookmark）功能」的完整鷹架。

按以下順序建立檔案：
資料庫 Schema → 查詢函式 → 服務層 → API 端點 → 前端元件 → 測試。

在寫程式碼之前先列出【完整的檔案清單】，等我確認後再動手。
遵循 CLAUDE.md 與現有程式碼慣例（Drizzle ORM、withAuth、ApiResponse<T>）。
```
