# A-01　五要素提示詞結構

> **類別**：A. 核心框架

---

```
角色：擔任資深 TypeScript / Next.js 開發者。
情境：這是一個使用 Next.js 16 + PostgreSQL（Neon）+ Tailwind CSS + Drizzle ORM 的 （框架） 專案。相關檔案為 （檔名）。
任務：實作文章詳情 API（GET /api/articles/（id）），回傳文章、作者資訊、留言數、是否已收藏。
限制：使用現有的 withAuth middleware；遵循 db/schema.ts 的 Drizzle schema 定義。
成功標準：回傳 ArticleDetail 型別（含 author, commentCount, isBookmarked）；未登入時 isBookmarked 為 false。
```
