# G-03　查詢優化

> **類別**：G. 資料庫

---

```
優化以下 SQL 查詢（目前執行時間：8 秒）：
SELECT a.*, u.name AS author_name, COUNT(c.id) AS comment_count FROM articles a LEFT JOIN users u ON a.author_id = u.id LEFT JOIN comments c ON c.article_id = a.id WHERE a.status = 'published' GROUP BY a.id, u.name ORDER BY a.created_at DESC;
Seq Scan on articles (cost=0.00..2840.00 rows=180) (actual time=0.021..4823.102 rows=180 loops=1) Buffers: shared hit=1240
資料量：（筆數） 筆
請提供：
1. 問題診斷（為什麼慢？）
2. 優化後的 SQL
3. 是否需要新增索引（哪個欄位？）
4. 預期改善幅度
```
