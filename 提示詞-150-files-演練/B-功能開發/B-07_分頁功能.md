# B-07　分頁功能

> **類別**：B. 功能開發

---

```
為 GET /api/articles 文章列表 實作分頁：
方式：Cursor-based（以 created_at + id 為 cursor）
預設每頁：20 筆
API 回傳格式：
{
  data: （...）,
  pagination: { total, page, limit, hasNext, nextCursor }
}
前端：無限捲動 或 頁碼導航（擇一）。
```
