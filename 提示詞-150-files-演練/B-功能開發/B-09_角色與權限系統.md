# B-09　角色與權限系統

> **類別**：B. 功能開發

---

```
實作 RBAC（角色為基礎的存取控制）：
角色清單：admin（管理員）、editor（編輯者）、reader（一般讀者）
資源清單：admin: 全部功能（含刪除任何文章）；editor: 發布/編輯所有文章；reader: 瀏覽、留言、收藏
實作方式：
- 資料庫：roles, permissions, user_roles 資料表
- 中介層：checkPermission(resource, action)
- 前端：根據權限隱藏/停用 UI 元素
```
