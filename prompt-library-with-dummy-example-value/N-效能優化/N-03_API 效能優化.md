# N-03　API 效能優化

> **類別**：N. 效能優化

---

```
這個 API 端點的 P95 延遲是 800ms，目標是 200ms：
GET /api/articles/（id） — 回傳文章詳情，目前未驗證 author_id 是否屬於當前使用者
請找出瓶頸：
- 有幾次資料庫查詢？能合併嗎？
- 有哪些計算可以快取？
- 有哪些外部 API 呼叫可以並行化？
- 回應資料是否有不必要的欄位？
```
