# H-05　GraphQL 查詢建構

> **類別**：H. API 串接

---

```
為 GitHub GraphQL API 建構查詢：
需求：取得指定 Repository 最近 20 個已關閉的 Issue，含標題、建立時間、關閉時間、標籤列表
Schema：（貼上相關的 Type 定義）
請撰寫：
- Query / Mutation / Subscription（依需求）
- Fragment（避免重複欄位）
- 變數定義（不要 hardcode 值）
- TypeScript 型別（用 GraphQL Code Generator）
```
